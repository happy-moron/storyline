I'd like to refactor how vocab and flashcards are stored within the 'books' directory. Currently flashcards, their vocab, and their images are stored in a subdirectory of the podcast episode where they were generated.

Better would be a single flashcard manifest which can use the Simplified Chinese words as the index/key. 

This unified manifest could be then used for other things as well. 

I'd like to come up with a design for organizing this.

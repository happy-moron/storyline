Currently the podcast structure is 'hard-coded' to expect a pair of hsk grammar points and a theme sentence (which provides the slug).

More flexible is to restructure the main podcast prompt to expect a more generic episode description block which for hsk-episodes would still be the theme phrase plus hsk grammar, but for other podcast types could be a freeform description or similar. 

Different podcast types could have different description constructors (e.g. )